# دليل الساهمة

شكراً لاهتمامك بالمساهمة في المكتبة الشيعية الرقمية!

## كيفية إضافة كتاب جديد

إضافة كتاب جديد إلى المكتبة سهلة جداً ولا تتطلب معرفة بالبرمجة.

### الخطوات:

#### 1. إنشاء مجلد الكتاب

أنشئ مجلد جديد في `books/` بصيغة `كتاب-اسم` (بالإنجليزية):

```
books/book-name/
```

#### 2. إضافة محتوى الكتاب

أنشئ ملف `content.txt` يحتوي على نص الكتاب بصيغة UTF-8:

```
books/book-name/content.txt
```

للكتب الكبيرة المقسمة لفصول، أنشئ مجلد `chapters/`:

```
books/book-name/chapters/chapter-1.txt
books/book-name/chapters/chapter-2.txt
...
```

#### 3. إنشاء صفحة الكتاب

أنشئ ملف `index.md` مع البيانات الوصفية:

```markdown
---
layout: book
book_id: book-name
title: عنوان الكتاب
author: اسم المؤلف
section: section-id
description: وصف قصير عن الكتاب
pages: 200
content_type: single
---

(محتوى الكتاب يجب أن يكون في content.txt)
```

#### 4. تحديث قائمة الكتب

أضف إدخال جديد في `_data/books.yml`:

```yaml
- id: book-name
  title: عنوان الكتاب
  author: اسم المؤلف
  section: section-id
  description: وصف قصير
  pages: 200
  content_type: single
  order: 11
```

### أقسام متاحة

- `quran` - القرآن الكريم
- `hadith` - الأحاديث الشريفة
- `tafseer` - التفسير والتأويل
- `fiqh` - الفقه الإسلامي
- `history` - التاريخ الإسلامي

### إضافة قسم جديد

لإضافة قسم جديد، أضف إدخال في قسم `sections` في `_data/books.yml`:

```yaml
- id: new-section
  title: عنوان القسم
  description: وصف القسم
  icon: book
  order: 6
```

## معايير الملفات

### ترميز الملفات

جميع ملفات النصوص يجب أن تكون بصيغة **UTF-8**.

### صيغة النصوص

- استخدم Markdown في ملفات `index.md`
- استخدم نصوص عادية في ملفات `content.txt`
- للفصول، استخدم عناوين ومحتوى واضح

### مثال على هيكل كتاب

```
books/example-book/
├── index.md
└── content.txt
```

أو للكتب المقسمة:

```
books/example-book/
├── index.md
└── chapters/
    ├── chapter-1.txt
    ├── chapter-2.txt
    └── chapter-3.txt
```

## الاختبار المحلي

للتأكد من أن كتابك يعمل بشكل صحيح:

```bash
gem install bundler
bundle install
bundle exec jekyll serve
```

ثم افتح http://localhost:4000 في متصفحك.

## النشر

عند الانتهاء:

1. أرسل Pull Request عبر GitHub
2. سيتم مراجعة الكتاب والمحتوى
3. بعد الموافقة، سيتم دمجه في الموقع الرسمي

## الأسئلة؟

إذا كان لديك أي أسئلة، يرجى فتح Issue على GitHub.

شكراً لمساهمتك! 📚
